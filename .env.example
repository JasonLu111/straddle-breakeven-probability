# Copy to .env (gitignored) and fill in once ORATS access is confirmed.
# Which ORATS product (History EOD / SMV Summaries / tick-level) determines the
# endpoint and field names used in src/data/load_options.py -- to be finalized
# once the subscription plan is confirmed.
ORATS_API_KEY=
